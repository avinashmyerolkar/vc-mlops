from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='hands on version control in mlops via emotion detection through text',
    author='avinash-m-yerolkar',
    license='',
)
